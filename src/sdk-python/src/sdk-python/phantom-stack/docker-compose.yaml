services:
  sdk-python:
    container_name: sdk-python
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    image: sdk-python:dev
    working_dir: /app
    environment:
      # Default AWS test credentials so boto3/moto based tests have a region.
      AWS_DEFAULT_REGION: us-west-2
    command: ["sleep", "infinity"]
