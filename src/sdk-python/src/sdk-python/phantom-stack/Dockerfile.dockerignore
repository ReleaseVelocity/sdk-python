# Exclude the recursive duplicate copy of the project to keep the build
# context small and avoid confusing the editable install.
src/sdk-python

# Standard Python / VCS noise
**/__pycache__
**/*.pyc
.git
.venv
build
dist
.mypy_cache
.pytest_cache
.ruff_cache
.coverage*
