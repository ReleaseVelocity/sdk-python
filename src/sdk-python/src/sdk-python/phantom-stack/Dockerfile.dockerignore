# Exclude the nested duplicate source tree to avoid copying it twice
src/sdk-python/

# Build / cache artifacts
**/__pycache__/
**/*.pyc
.mypy_cache/
.pytest_cache/
.ruff_cache/
build/
dist/
.venv/
.coverage*

# The build initialises its own git repo; ignore any host VCS metadata
.git/
