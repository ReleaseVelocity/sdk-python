services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    image: app
    container_name: strands-sdk-python
    working_dir: /app
    command: sleep infinity
    environment:
      # Provide dummy AWS region so boto3-based code/imports don't complain
      AWS_DEFAULT_REGION: us-west-2
