services:
  sdk-python:
    image: app
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    pull_policy: never
    container_name: sdk-python
    working_dir: /app
    command: ["sleep", "infinity"]
