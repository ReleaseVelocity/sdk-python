services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
      args:
        STRANDS_PRETEND_VERSION: "0.0.0"
    image: strands-sdk-python:dev
    container_name: strands-sdk-python
    working_dir: /app
    environment:
      AWS_DEFAULT_REGION: us-west-2
      LOG_LEVEL: INFO
    command: sleep infinity
