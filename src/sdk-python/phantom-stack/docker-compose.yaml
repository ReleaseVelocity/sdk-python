services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
      network: host
    container_name: sdk-python
    working_dir: /app
    # Keep the container alive so a developer can exec in and run
    # tests, linters, mypy, etc.
    command: ["sleep", "infinity"]
