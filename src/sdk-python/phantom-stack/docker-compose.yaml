services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    image: app
    container_name: strands-sdk-python
    working_dir: /app
    environment:
      # Default region used by moto-mocked AWS tests
      - AWS_DEFAULT_REGION=us-west-2
    command: sleep infinity
