services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
      network: host
    image: app
    container_name: sdk-python
    working_dir: /app
    command: ["sleep", "infinity"]
