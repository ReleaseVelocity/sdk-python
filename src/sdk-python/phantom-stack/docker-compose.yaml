services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    image: app
    container_name: strands-sdk-python
    working_dir: /app
    command: sleep infinity
    environment:
      # Mock AWS credentials so boto3/moto-based tests have a default region.
      AWS_DEFAULT_REGION: us-west-2
