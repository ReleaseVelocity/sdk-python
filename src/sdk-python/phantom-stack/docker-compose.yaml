services:
  sdk-python:
    image: sdk-python:latest
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    container_name: sdk-python
    working_dir: /app
    environment:
      # Keep AWS clients/moto happy without real credentials.
      AWS_DEFAULT_REGION: us-west-2
    command: ["sleep", "infinity"]
