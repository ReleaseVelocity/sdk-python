services:
  sdk-python:
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
      network: host
      args:
        PKG_VERSION: "0.0.0"
    container_name: strands-sdk-python
    working_dir: /app
    command: ["sleep", "infinity"]
