services:
  sdk-python:
    image: app
    build:
      context: ..
      dockerfile: phantom-stack/Dockerfile
    container_name: sdk-python
    environment:
      - SETUPTOOLS_SCM_PRETEND_VERSION=0.1.0.dev0
      - AWS_DEFAULT_REGION=us-west-2
    working_dir: /app
    command: sleep infinity
