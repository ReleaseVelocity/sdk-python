# Exclude the nested duplicate project tree to keep the build context small.
src/sdk-python/

# VCS and caches
.git/
**/__pycache__/
**/*.pyc
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
build/
dist/
.coverage*

# Our own stack dir
phantom-stack/
