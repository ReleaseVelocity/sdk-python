# Build-context excludes for the phantom-stack image build.
# Keep the context lean and avoid copying the nested duplicate tree.
**/__pycache__
**/*.pyc
.git
build
dist
.venv
.mypy_cache
.pytest_cache
.ruff_cache
.coverage*
# Nested duplicate copy of the project tree (not the real project).
src/sdk-python
phantom-stack
