# Override dockerignore for the build context (project root has no .dockerignore).
# Keep everything needed for dev + tests; exclude only churn/large caches.
.git
**/__pycache__
**/*.pyc
.venv
.mypy_cache
.pytest_cache
.ruff_cache
build
dist
phantom-stack
